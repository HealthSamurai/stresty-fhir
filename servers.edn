{ns servers
 import #{sty fhir.FHIR4-0-1.Patient-Track.read
              fhir.FHIR4-0-1.Patient-Track.vread
              fhir.FHIR4-0-1.Patient-Track.create}

 aidbox
 {:zen/tags #{sty/env}
  :base-url "https://edgeniquola.edge.aidbox.app"
  :basic-auth {:user "basic" :password "secret"}}

  ;; local
  ;; {:zen/tags #{sty/env}
  ;;  :base-url "http://devbox:8080"
  ;;  :basic-auth {:user "root" :password "secret"}}


 }
